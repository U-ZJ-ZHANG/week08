name: Frontend CI

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check frontend files
        run: ls -l frontend

      - name: Skip frontend tests
        run: echo "No frontend build step required (static HTML + nginx)"
